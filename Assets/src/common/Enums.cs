using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public partial class sm
{
    public enum even
    {
        TURN_UPDATE = 1,
        BOX_UPDATE = 2,
        CHECK_FINISH = 3,
    }
    public enum audio
    {

    }
}
